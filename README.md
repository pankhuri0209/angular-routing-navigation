# angular-routing-navigation

<p>
  The src zip is developed in Angular Application 11, the code can be referred to create listing in angular and select an option from list and change the url nvaigation.
  </p>

Installation done--
1. npm install --save @angular/material @angular/animations @angular/cdk
2. npm install --save hammerjs
